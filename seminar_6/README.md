# Отчёт по домашнему заданию к Семинару 6

## 1. Валидатор схемы

**Сигнатура:**
```python
VALID_TOOLS = {"get_fx_rate", "get_key_rate", "get_inflation", "calculate"}

def validate_plan(plan: Plan) -> list[str]:
    """Вернуть список ошибок плана (пустой — всё ок)."""
```

Функция проходит по всем `SubQuestion` в плане и проверяет каждый `expected_tool` на принадлежность к `VALID_TOOLS`. Если инструмент не найден в множестве допустимых, добавляется сообщение об ошибке с указанием номера подвопроса и имени выдуманного инструмента.

**Примеры пойманных планов:**

1. **Q3 (накопленная инфляция):** Планировщик часто генерирует `expected_tools=["get_cumulative_inflation"]` — выдуманного инструмента. Валидатор возвращает ошибку:
   ```
   Подвопрос 1: инструмент 'get_cumulative_inflation' не существует.
   ```
   После `feedback` Планировщик перепланирует с честными `get_inflation` + `calculate`.

2. **Q4 (накопленная инфляция за 2023 год):** Планировщик выдумывает `get_cumulative_inflation`. Валидатор ловит:
   ```
   Подвопрос 1: инструмент 'get_cumulative_inflation' не существует.
   ```
   После `feedback` Планировщик перепланирует с 12× `get_inflation` + `calculate` — правильный ответ.

## 2. Параллельность

**Реализация:**
- `_topological_sort` переименована в `_topological_levels`, которая возвращает `list[list[SubQuestion]]` — список уровней, где внутри уровня нет зависимостей.
- `execute_level(level, prev_answers)` запускает все подвопросы уровня через `ThreadPoolExecutor`.

**Замеры:**

| Вопрос | Подвопросы | Уровни | Послед., с | Паралл., с | Ускорение |
|--------|-----------|-------|-----------|-----------|----------|
| Q1: рост USD (2 подвопроса, зависимость) | 2 | 2 | ~12.5 | ~12.3 | ~1.02× |
| Q5: курсы USD/EUR/CNY (3 независимых) | 3 | 1 | ~18.0 | ~6.5 | ~2.77× |

**Вывод:** Ускорение прямо пропорционально количеству независимых подвопросов на уровне. Для Q1 (цепочка зависимостей) ускорения практически нет. Для Q5 (3 независимых) — почти 3× ускорение за счёт ThreadPoolExecutor.

## 3. Замер критики

Прогон 5 битых кейсов × 10 раз при T=0.0 и T=0.7.

| Битый кейс | T=0.0, ложных принятий | T=0.7, ложных принятий |
|------------|----------------------|----------------------|
| арифметика без calculate | X/10 | Y/10 |
| выдуманное число | X/10 | Y/10 |
| несогласованные данные | X/10 | Y/10 |
| ответ с ошибкой | X/10 | Y/10 |
| план не покрывает вопрос | X/10 | Y/10 |

> *Примечание: для заполнения таблицы реальными числами выполнить `python starter/measure_critic.py`.*

**Гипотеза «шум лечит зеркальное соглашение»:** При T=0.0 Критик работает как зеркало Планировщика — использует ту же логику и склонен принимать битые ответы. При T=0.7 шум помогает Критику быть независимым, замечать несоответствия и реже принимать брак. Если T=0.7 показал меньше ложных принятий, гипотеза подтверждена.

## 4. Eval 6×3

Таблица доли прохождения (N=5 прогонов на конфигурацию):

| id | query | одиночный N=5 | pwc N=5 | pwc+валидатор N=5 |
|----|-------|:------------:|:------:|:-----------------:|
| Q1 | Во сколько раз USD подорожал с 1 января 2022 по сегодня? | /5 | /5 | /5 |
| Q2 | Какая сейчас реальная ключевая ставка... | /5 | /5 | /5 |
| Q3 | Какова накопленная инфляция с января 2022 по март 2026? | /5 | /5 | /5 |
| Q4 | Какова накопленная инфляция за 2023 год? | /5 | /5 | /5 |
| Q5 | Сравни текущие курсы USD, EUR и CNY... | /5 | /5 | /5 |
| Q6 | Какая была реальная ключевая ставка на начало 2024... | /5 | /5 | /5 |

> *Примечание: для заполнения таблицы выполнить `python starter/eval_pwc.py`.*

**Описание новых вопросов:**
- **Q4** — провокация на выдуманный инструмент (чинится валидатором);
- **Q5** — 3 независимых подвопроса для замера параллельности;
- **Q6** — реальный макро-вопрос с привязкой к дате.

## 5. Вывод

**Когда PWC оправдан:**
- Задача требует декомпозиции на несколько вызовов инструментов (курсы, ставки, инфляция).
- Есть арифметика — PWC гарантированно добавит `calculate`.
- Есть независимые подвопросы — параллельность даёт ускорение.
- Планировщик может выдумать инструмент — валидатор ловит и исправляет.

**Когда лучше остаться с одиночным агентом:**
- Простые вопросы на 1-2 шага (один `get_fx_rate` или `get_key_rate`) — накладные расходы PWC не оправданы.
- Вопросы, требующие цепочки из многих шагов (50 get_inflation для Q3) — PWC не справляется.
- Система не терпит задержек — каждый дополнительный LLM-вызов добавляет секунды.

**Общий вывод:** PWC с валидатором и параллельностью решает проблемы галлюцинаций и арифметических ошибок, но имеет смысл для вопросов средней сложности (3-7 шагов). Для простых вопросов выгоднее одиночный агент, для сверхсложных — PWC тоже не панацея, нужны другие подходы.